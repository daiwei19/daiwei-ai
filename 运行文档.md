# 健康咨询管理系统 - 运行文档

## 一、项目概述

本项目是一个**健康咨询管理系统**，基于 Node.js + Express + MySQL 构建，提供用户注册/登录、健康数据管理、症状咨询分析等功能。

### 功能模块

| 模块 | 描述 |
|------|------|
| 用户认证 | 登录、注册 |
| 个人资料 | 管理用户基本信息 |
| 健康数据 | 体温、血压、心率、血糖等数据录入与分析 |
| 症状咨询 | AI辅助症状分析与建议 |
| 管理员面板 | 用户管理、数据查看与导出 |

---

## 二、环境要求

| 依赖 | 版本要求 | 说明 |
|------|----------|------|
| Node.js | >= 14.0.0 | 运行环境 |
| MySQL | >= 5.7 | 数据库 |
| npm | >= 6.0.0 | 包管理工具 |

---

## 三、安装步骤

### 1. 克隆项目

```bash
# 进入项目目录
cd c:\ai\yishen-AI
```

### 2. 安装依赖

```bash
npm install
```

### 3. 配置数据库

#### 方式一：使用默认配置（推荐）

系统默认配置如下，确保 MySQL 服务已启动：

```javascript
// 默认数据库配置
host: 'localhost',
port: 3306,
user: 'root',
password: '123456',
database: 'health_system'
```

#### 方式二：使用环境变量

创建 `.env` 文件（可选）：

```env
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=123456
DB_NAME=health_system
```

### 4. 测试数据库连接

```bash
node check-mysql.js
```

**成功输出示例：**
```json
{"ok":true,"version":"8.0.33"}
```

---

## 四、运行步骤

### 1. Windows PowerShell 启动

在 VS Code 的终端中进入项目目录：

```powershell
cd c:\ai\yishen-AI
```

Windows PowerShell 可能会因为执行策略禁止运行 `npm.ps1`。为避免该问题，使用 `npm.cmd` 启动：

```powershell
npm.cmd start
```

启动成功后会看到类似输出：

```
MySQL connection successful
Server running at http://localhost:3000
```

### 2. 其他终端启动

```bash
npm start
```

如果使用的是 Windows 命令提示符（CMD），也可以执行：

```bat
npm start
```

### 3. 使用 VS Code 任务启动

工作区已配置“启动健康咨询系统（Windows）”任务。可以通过 VS Code 菜单选择：

```
终端 -> 运行任务 -> 启动健康咨询系统（Windows）
```

该任务实际执行 `npm.cmd start`，适用于 PowerShell 执行策略限制 `npm.ps1` 的情况。

### 4. 服务启动成功

```
MySQL connection successful
Server running at http://localhost:3000
```

### 5. 访问地址

| 页面 | URL | 说明 |
|------|-----|------|
| 登录页 | http://localhost:3000 | 用户登录/注册入口 |
| 用户仪表盘 | http://localhost:3000/dashboard.html | 用户功能页面 |
| 管理员面板 | http://localhost:3000/admin.html | 管理员功能页面 |

### 6. 验证服务状态

服务启动后，可以在 PowerShell 中执行以下命令检查接口：

```powershell
Invoke-WebRequest -UseBasicParsing http://localhost:3000/api/health | Select-Object -ExpandProperty Content
```

正常响应示例：

```json
{"success":true,"message":"服务已启动","database":"connected"}
```

也可以直接在浏览器打开登录页：

```
http://localhost:3000
```

---

## 五、默认账号

系统启动时会自动创建以下测试账号：

| 角色 | 用户名 | 密码 | 说明 |
|------|--------|------|------|
| 管理员 | admin | admin123 | 管理所有用户和数据 |
| 普通用户 | user1 | 123456 | 普通用户权限 |

---

## 六、功能说明

### 1. 用户端功能（dashboard.html）

#### 个人资料管理
- 修改姓名、性别、年龄、身高、体重、联系方式

#### 健康数据录入
| 指标 | 单位 | 正常范围 |
|------|------|----------|
| 体温 | °C | 36.0 - 37.5 |
| 血压 | mmHg | 90/60 - 140/90 |
| 心率 | 次/分钟 | 60 - 100 |
| 血糖 | mmol/L | 3.9 - 7.0 |
| 胆固醇 | mmol/L | ≤ 5.2 |
| 血氧饱和度 | % | ≥ 95 |

#### 症状咨询
- 输入症状描述（如：头痛、咳嗽、腹痛等）
- 选择症状持续时间
- AI自动分析并给出建议

### 2. 管理员端功能（admin.html）

#### 用户管理
- 查看所有用户列表
- 编辑用户信息
- 删除用户

#### 健康记录管理
- 查看所有用户健康数据
- 按用户筛选
- 导出数据

#### 咨询记录管理
- 查看所有咨询记录
- 按用户筛选

---

## 七、数据库结构

### 表结构

#### users（用户表）
| 字段 | 类型 | 说明 |
|------|------|------|
| id | INT | 主键，自增 |
| username | VARCHAR(100) | 用户名，唯一 |
| password | VARCHAR(255) | 密码 |
| email | VARCHAR(255) | 邮箱 |
| role | VARCHAR(20) | 角色：admin/user |
| created_at | TIMESTAMP | 创建时间 |

#### profiles（用户资料表）
| 字段 | 类型 | 说明 |
|------|------|------|
| user_id | INT | 外键，关联users.id |
| name | VARCHAR(100) | 姓名 |
| gender | VARCHAR(20) | 性别 |
| age | INT | 年龄 |
| height | INT | 身高(cm) |
| weight | INT | 体重(kg) |
| phone | VARCHAR(50) | 电话 |

#### health_records（健康记录表）
| 字段 | 类型 | 说明 |
|------|------|------|
| id | INT | 主键，自增 |
| user_id | INT | 外键，关联users.id |
| temp | DECIMAL(4,1) | 体温 |
| blood_pressure | VARCHAR(50) | 血压 |
| heart_rate | INT | 心率 |
| blood_sugar | DECIMAL(4,1) | 血糖 |
| cholesterol | DECIMAL(4,1) | 胆固醇 |
| oxygen | INT | 血氧饱和度 |

#### consultations（咨询记录表）
| 字段 | 类型 | 说明 |
|------|------|------|
| id | INT | 主键，自增 |
| user_id | INT | 外键，关联users.id |
| symptoms | TEXT | 症状描述 |
| duration | VARCHAR(50) | 持续时间 |
| other_symptoms | TEXT | 其他症状 |
| analysis_json | TEXT | 分析结果JSON |

---

## 八、API 接口说明

### 基础路径：`/api`

#### 1. 健康检查

```
GET /api/health
```

**响应：**
```json
{
  "success": true,
  "message": "服务已启动",
  "database": "connected"
}
```

#### 2. 用户登录

```
POST /api/auth/login
Content-Type: application/json
```

**请求体：**
```json
{
  "username": "admin",
  "password": "admin123"
}
```

**响应：**
```json
{
  "success": true,
  "user": {
    "id": 1,
    "username": "admin",
    "email": "admin@health.com",
    "role": "admin"
  }
}
```

#### 3. 用户注册

```
POST /api/auth/register
Content-Type: application/json
```

**请求体：**
```json
{
  "username": "newuser",
  "password": "123456"
}
```

#### 4. 获取用户列表（管理员）

```
GET /api/users
```

#### 5. 更新用户信息

```
PUT /api/users/:id
Content-Type: application/json
```

**请求体：**
```json
{
  "username": "newname",
  "email": "new@example.com"
}
```

#### 6. 删除用户

```
DELETE /api/users/:id
```

#### 7. 获取个人资料

```
GET /api/profiles/:userId
```

#### 8. 更新个人资料

```
PUT /api/profiles/:userId
Content-Type: application/json
```

**请求体：**
```json
{
  "name": "张三",
  "gender": "男",
  "age": 30,
  "height": 175,
  "weight": 70,
  "phone": "13800138000"
}
```

#### 9. 获取健康记录

```
GET /api/health-records?userId=1
```

#### 10. 添加健康记录

```
POST /api/health-records
Content-Type: application/json
```

**请求体：**
```json
{
  "userId": 1,
  "temp": 36.8,
  "bloodPressure": "120/80",
  "heartRate": 75,
  "bloodSugar": 5.2,
  "cholesterol": 4.5,
  "oxygen": 98
}
```

#### 11. 删除健康记录

```
DELETE /api/health-records/:id
```

#### 12. 获取咨询记录

```
GET /api/consultations?userId=1
```

#### 13. 添加咨询记录

```
POST /api/consultations
Content-Type: application/json
```

**请求体：**
```json
{
  "userId": 1,
  "symptoms": "头痛、头晕",
  "duration": "3天",
  "otherSymptoms": "无",
  "analysis": {}
}
```

---

## 九、常见问题

### 1. MySQL 连接失败

**现象：**
```
MySQL connection failed, continuing without DB: connect ECONNREFUSED 127.0.0.1:3306
```

**解决方案：**
- 确保 MySQL 服务已启动
- 检查数据库连接配置（用户名、密码、端口）
- 确保 MySQL 允许本地连接

### 2. 端口被占用

**现象：**
```
Error: listen EADDRINUSE: address already in use :::3000
```

**解决方案：**
```bash
# Windows 查找占用端口的进程
netstat -ano | findstr :3000

# 终止进程（替换PID）
taskkill /F /PID <PID>

# 或使用其他端口
set PORT=3001 && npm start
```

### 3. 页面无法访问

**解决方案：**
- Windows PowerShell 确认服务已启动：`npm.cmd start`
- 其他终端确认服务已启动：`npm start`
- 确认访问地址正确：`http://localhost:3000`
- 使用健康检查接口确认服务状态：`http://localhost:3000/api/health`
- 检查防火墙设置

### 4. PowerShell 提示禁止运行脚本

**现象：**
```
无法加载文件 npm.ps1，因为在此系统上禁止运行脚本
```

**解决方案：**

直接使用 Windows 命令版本：

```powershell
npm.cmd start
```

也可以改用 VS Code 的“启动健康咨询系统（Windows）”任务运行项目。

---

## 十、技术栈

| 分类 | 技术 | 版本 |
|------|------|------|
| 后端框架 | Express | ^4.19.0 |
| 数据库 | MySQL | ^3.9.0 |
| 数据库驱动 | mysql2 | ^3.9.0 |
| 前端 | HTML5 | - |
| 样式 | CSS3 | - |
| 脚本 | JavaScript ES6+ | - |

---

## 十一、项目文件结构

```
yishen-AI/
├── index.html          # 登录页面
├── dashboard.html      # 用户仪表盘
├── admin.html          # 管理员面板
├── server.js           # 服务器端代码
├── script.js           # 前端脚本
├── style.css           # 样式文件
├── check-mysql.js      # MySQL连接测试
├── package.json        # 项目配置
└── node_modules/       # 依赖包（已安装）
```

---

**文档版本**: v1.1  
**创建时间**: 2026年7月  
**项目名称**: 健康咨询管理系统