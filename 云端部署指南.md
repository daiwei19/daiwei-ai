# 健康咨询系统 - 云端部署指南

## 一、问题排查

### 1.1 访问失败原因分析

如果你看到 "无法访问此页面" 或 "ERR_CONNECTION_RESET" 错误，可能原因：

| 问题 | 原因 | 解决方案 |
|------|------|----------|
| 连接重置 | 服务器启动失败 | 检查 Vercel 日志 |
| 500 错误 | 代码错误 | 检查构建日志 |
| 404 错误 | 路由配置错误 | 检查 vercel.json |
| 数据库连接失败 | 环境变量未配置 | 配置 DATABASE_URL |

### 1.2 检查 Vercel 日志

1. 登录 [Vercel Dashboard](https://vercel.com/dashboard)
2. 选择你的项目
3. 点击 **Deployments** 标签
4. 查看最新部署的 **Logs**

---

## 二、重新部署步骤

### 2.1 准备项目文件

确保项目包含以下文件：
```
yishen-AI/
├── server.js          # 后端服务
├── package.json       # 依赖配置  
├── vercel.json        # Vercel 配置
├── index.html         # 登录页面
├── dashboard.html     # 用户仪表盘
├── admin.html         # 管理员面板
├── script.js          # 前端逻辑
└── style.css          # 样式文件
```

### 2.2 通过 Vercel 控制台部署

**步骤 1：登录 Vercel**

1. 访问 [Vercel Dashboard](https://vercel.com/dashboard)
2. 使用 GitHub/Google 账号登录

**步骤 2：导入项目**

1. 点击 **Add New Project**
2. 选择 **Import Git Repository**
3. 选择你的 GitHub 仓库
4. 点击 **Import**

**步骤 3：配置项目**

1. **Project Name**: 输入项目名称（如 `health-system`）
2. **Framework**: 选择 `Other`
3. **Build Command**: 留空
4. **Output Directory**: 留空
5. 点击 **Deploy**

**步骤 4：配置环境变量**

部署成功后：

1. 进入项目设置页面
2. 点击 **Environment Variables**
3. 添加以下变量（如果使用数据库）：
   - `DATABASE_URL` = 你的数据库连接字符串

### 2.3 手动部署（使用 CLI）

如果 CLI 命令有问题，可以尝试：

```bash
# 卸载旧版本
npm uninstall -g vercel

# 安装最新版本
npm install -g vercel@latest

# 重新登录
vercel login --github

# 部署
vercel --prod
```

---

## 三、测试部署

### 3.1 测试基础功能

1. 访问部署地址（如 `https://your-project.vercel.app`）
2. 测试登录：
   - 用户名：`admin`
   - 密码：`admin123`
3. 测试注册功能

### 3.2 默认账号

| 角色 | 用户名 | 密码 |
|------|--------|------|
| 管理员 | admin | admin123 |
| 测试用户 | user1 | 123456 |

---

## 四、配置文件说明

### 4.1 vercel.json

```json
{
  "version": 2,
  "builds": [
    {
      "src": "server.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "server.js" },
    { "src": "/(.*\\.(html|css|js|png|jpg|gif|ico))$", "dest": "$1" },
    { "src": "/(.*)", "dest": "server.js" }
  ]
}
```

### 4.2 package.json

```json
{
  "name": "health-consultation-system",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js"
  },
  "dependencies": {
    "express": "^4.19.0"
  }
}
```

---

## 五、常见问题解决

### 5.1 部署失败

**问题**: Vercel 部署显示失败

**解决方案**:
1. 检查 `package.json` 是否正确配置
2. 确保 `server.js` 没有语法错误
3. 检查 Node.js 版本（Vercel 默认使用 LTS 版本）

### 5.2 页面无法访问

**问题**: 显示 "ERR_CONNECTION_RESET"

**解决方案**:
1. 检查服务器是否正常启动
2. 查看 Vercel 函数日志
3. 确保端口配置正确（使用 `process.env.PORT`）

### 5.3 API 调用失败

**问题**: 前端无法调用后端 API

**解决方案**:
1. 检查 API 路径是否正确
2. 确保 CORS 配置正确
3. 检查网络连接

---

## 六、本地测试

在部署前，可以先在本地测试：

```bash
# 安装依赖
npm install

# 启动服务器
node server.js

# 访问 http://localhost:3000
```

---

## 七、联系支持

如果问题仍未解决：

1. 查看 [Vercel 帮助文档](https://vercel.com/docs)
2. 检查 [Vercel 状态页面](https://status.vercel.com/)
3. 在 Vercel Dashboard 中提交支持请求